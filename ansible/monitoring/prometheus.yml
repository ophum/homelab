---
- name: Set up Prometheus
  hosts: prometheus
  remote_user: ubuntu
  become: true
  vars:
    prometheus_config_flags_extra:
      web.enable-remote-write-receiver: null
  roles:
    - ../clickhouse/roles/hosts
    - prometheus.prometheus.prometheus
    - prometheus.prometheus.node_exporter
    - rsyslog_config
